# Vagrant file for VM setup

## steps to setup VM on window

- install virtual box
- install vagrant 
- cd to folder containing Vagrantfile
- run below command 
	vagrant up

## vagrant commands
- vagrant up
- vagrant status
- vagrant destroy
- vagrant halt
